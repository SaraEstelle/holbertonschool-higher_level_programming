// Séléctionne le prmier élément<header> dans le DOM
// et le sotkce dnas une constante pour pouvoir le manipuler
const header = document.querySelector('header');

// Modifie la couleur du texte de l'éméement header en rouge
// en utilisant la proporiété style.color avec le code héxadécimal #FF0000
header.style.color = '#FF0000';
